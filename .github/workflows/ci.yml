name: CI Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Build and Verify with Maven
        run: xvfb-run mvn clean verify -Pjacoco

      - name: Upload coverage to Coveralls
        env:
          COVERALLS_REPO_TOKEN: ${{ secrets.COVERALLS_REPO_TOKEN || secrets.COVERALLSTOKEN || secrets.COVERALLS_TOKEN }}
        run: mvn coveralls:report -DrepoToken=${{ secrets.COVERALLS_REPO_TOKEN || secrets.COVERALLSTOKEN || secrets.COVERALLS_TOKEN }}

      - name: SonarCloud analysis
        env:
          SONAR_TOKEN: ${{ secrets.EVENTTICKETINGTOKEN || secrets.SONARTICKETINGTOKEN || secrets.SONAR_TOKEN || secrets.SONARCLOUDTOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: >
          mvn sonar:sonar
          -Dsonar.organization=janneh24
          -Dsonar.projectKey=Janneh24_Event-Ticketing-Application
          -Dsonar.host.url=https://sonarcloud.io
          -Dsonar.token=${{ secrets.EVENTTICKETINGTOKEN || secrets.SONARTICKETINGTOKEN || secrets.SONAR_TOKEN || secrets.SONARCLOUDTOKEN }}
